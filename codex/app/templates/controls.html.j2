<div class="btn-group mx-3 float-end control-buttons"
     role="group"
     aria-label="Controls"
     data-cdxid="{{ codex._id }}"
     data-cdxurl="{{ request.host_url }}/{{ codex._id }}">
    <input type="checkbox"
           class="btn-check show-comments"
           id="btn-show-comments-{{ codex._id }}"
           checked
           autocomplete="off">
    <label class="btn btn-outline-primary"
           for="btn-show-comments-{{ codex._id }}">Hide Comments</label>
    <div class="btn-group" role="group">
        <button type="button"
                class="btn btn-primary dropdown-toggle"
                data-bs-toggle="dropdown"
                aria-expanded="false">Download/Share</button>
        <ul class="dropdown-menu">
            <li>
                <a class="dropdown-item copy-link">Copy Link</a>
            </li>
            <li>
                <a class="dropdown-item copy-id">Copy ID</a>
            </li>
            <li>
                <a class="dropdown-item copy-contents" {{ 'hidden' if codex_type != 'file' }}>Copy Contents</a>
            </li>
            <li>
                <a class="dropdown-item"
                   data-bs-toggle="modal"
                   data-bs-target="#downloadModal-{{ codex._id }}">Download</a>
            </li>
        </ul>
    </div>
</div>
{% include "download.html.j2" %}
